export interface SocialLink {
  id: string;
  name: string;
  url: string;
  icon: string;
}

export interface Bot {
  id: string;
  name: string;
  description: string;
  placeholder: string;
  icon?: string;
} 